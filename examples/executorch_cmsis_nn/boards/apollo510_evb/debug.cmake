# Role fragment: debug. Debug-probe / SEGGER device facts.
# This board uses the toolchain default device, so this fragment sets
# nothing; boards with a specific probe device set NSX_SEGGER_DEVICE here.
